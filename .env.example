# Profile usado pelo Docker Compose (local, dev, hml, prod)
PROFILE=local

# URL do WebSocket da API
# Em desenvolvimento local fora do docker: ws://localhost:3001
# No Docker Compose, o browser do cliente ainda acessa via localhost
VITE_API_URL=ws://localhost:3001
