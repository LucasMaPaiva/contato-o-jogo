services:
  app:
    build:
      context: ..
      dockerfile: .docker/Dockerfile
    env_file:
      - ../.env.local
    ports:
      - "3000:3000"
    networks:
      - h7-network

networks:
  h7-network:
    driver: bridge
